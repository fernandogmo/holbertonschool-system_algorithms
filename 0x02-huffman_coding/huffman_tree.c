#include "huffman.h"

/**
 * huffman_tree - description
 * @data: data
 * @freq: freq
 * @size: size
 * Return: int
 */
binary_tree_node_t *huffman_tree(char *data, size_t *freq, size_t size)
{
	return (NULL);
}
